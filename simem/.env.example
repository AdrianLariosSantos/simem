# Database Configuration
DB_ENGINE=django.db.backends.postgresql
DB_NAME=simem_db
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_HOST=localhost
DB_PORT=5432

# Django Settings
DEBUG=True
SECRET_KEY=your-secret-key-here
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# Application Settings
ENVIRONMENT=development

# CORS Configuration - Para desarrollo se permite todos los orígenes
# Para producción, especificar orígenes permitidos en CORS_ALLOWED_ORIGINS

